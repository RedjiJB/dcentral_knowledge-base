# Knowledge Base — Materialized Index (Stages 3-6)

Regenerated by `scripts/rebuild_index.py` from `registry/materialized-manifest.json` (itself built by `scripts/build_manifest.py` from `_kb_doc_classified.jsonl`, `_topics.md`, and `registry/consolidated-topics.json`). Don't hand-edit -- rerun the two scripts instead.

## Stage 3 — classification (live docs only, excludes `_superseded/`)

| Category | Doc count | Projects |
|---|---|---|
| `academic-personal` | 25 | 6 |
| `business-legal` | 98 | 12 |
| `core/economics` | 12 | 5 |
| `core/governance` | 20 | 6 |
| `core/identity` | 17 | 5 |
| `core/observability` | 3 | 2 |
| `haiti-diaspora` | 39 | 5 |
| `hardware/campus` | 3 | 2 |
| `hardware/sensing-planes` | 12 | 4 |
| `hardware/shi-node` | 1 | 1 |
| `mesh-services/ai` | 8 | 4 |
| `mesh-services/compute` | 2 | 1 |
| `mesh-services/connectivity` | 2 | 1 |
| `mesh-services/sensors-mobility` | 10 | 1 |
| `meta/platform-scaffolding` | 42 | 10 |
| `meta/simulation` | 10 | 0 |
| `meta/status-tracking` | 9 | 3 |
| `security` | 73 | 8 |
| `verticals/commerce` | 2 | 1 |
| `verticals/home-trades` | 2 | 1 |
| `verticals/social-comm` | 8 | 2 |

## Stage 4 — dedup / status

- **396** canonical (live, no dedup issue)
- **74** superseded (moved to a `_superseded/` subfolder, each with a `duplicate_of`/`superseded_by` pointer verified by `scripts/check_drift.py`)
- **2** disputed (`status: disputed` — a genuine "no basis to prefer one" case per DC-DEDUP-STD-001 §4, left in place rather than forced)
- **472** total docs tracked

## Stage 5 — topic clusters

- **80** confirmed topics in [`_topics.md`](./_topics.md)
- **302** canonical docs belong to at least one topic (367 total topic memberships — 65 docs are deliberately cross-listed in more than one topic; see each doc's `consolidated_into` field)
- **96** canonical docs correctly left ungrouped (no other doc shares their subject closely enough to cluster)

## Stage 6 — consolidator output

- **80** `DC-*-RECONCILED-*.md` documents tied to a confirmed Stage 5 topic in [`docs/`](../docs/) — see `registry/consolidated-topics.json` for the topic → doc mapping
- **1** more `DC-*-RECONCILED-*.md` document(s) predate the topic-registry-driven process (DC-DION-RECONCILED-001.md) — a one-off consolidation done before `registry/consolidated-topics.json` existed, not tied to any of the 80 topics
- **17** other standalone documents also live in `docs/` (pre-pipeline reference docs, conversation-artifact extractions such as `DC-LKB-*`/`DC-SIM-*`, and later additions not produced by this pipeline)

## Provenance chain, per document

Every live doc's own front matter now carries the full chain: `topic:` (Stage 5) → `consolidated_into:` (Stage 6, added by `scripts/add_consolidated_into.py`). Every superseded doc's front matter carries `status`, `duplicate_of`/`superseded_by`, and a reason (Stage 4). See `registry/materialized-manifest.json` for the same information in one machine-readable file, keyed by path.
