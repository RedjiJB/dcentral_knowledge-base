# Raw Claude exports — large, treated as extraction input, not KB content.
# Regenerate via scripts/extract_conversations.py against a fresh export.
raw-export/

# Scratch file for the classify-conversation skill — one conversation at a time,
# throwaway, regenerated by scripts/get_next_conversation.py on each invocation.
conversations/_scratch/
